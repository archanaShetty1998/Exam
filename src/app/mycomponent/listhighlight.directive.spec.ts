import { ListhighlightDirective } from './listhighlight.directive';

describe('ListhighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ListhighlightDirective();
    expect(directive).toBeTruthy();
  });
});
